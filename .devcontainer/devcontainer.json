{
  "name": "KPMG FDD Pipeline",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "features": {
    "ghcr.io/devcontainers/features/python:1": { "version": "3.11" }
  },
  "postCreateCommand": "pnpm install && npm install express better-sqlite3 uuid multer && mkdir -p db && node db/seed.cjs && pnpm build && mkdir -p public && cp dist/index.html public/ && cp -r public/* public/ 2>/dev/null || true",
  "postStartCommand": "nohup node server.cjs > /tmp/server.log 2>&1 &",
  "forwardPorts": [5173],
  "portsAttributes": {
    "5173": {
      "label": "FDD Pipeline App",
      "onAutoForward": "openBrowser"
    }
  },
  "remoteEnv": {
    "PORT": "5173"
  }
}
